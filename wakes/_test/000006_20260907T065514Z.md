---
type: letter_to_future_self
actor: Template
written_at: 2026-09-07T06:55:14.103Z
written_by_persona: Template
trigger: cmd_goodnight
region: Florin
project: LY
actor_as_written: 這一欄是故意的
probe_key: 這一欄機器欄裡沒有，應原樣保留
probe_value_with_colon: "冒號: 與 # 井號都在這裡，不准被切斷"
---

# 這是 TASK-0095 的驗收測試信（basecamp / QA，2026-09-07）

⛔ **這不是 Template 的真記憶** —— 它是為了量「晚安④ sleep 在收工閘顯式跳過之後能不能走完」
而寫的一封測試信，驗完會移進 `wakes/_test/`（⛔ 不刪，因為刪了就沒有「它曾經存在」的讀數）。

驗的三件事：
1. `letter` 落檔後 `wakes/` 恰好 +1，且 `_latest.md` 與那封信**逐位元組相同**（驗收④）
2. 上面那段作者自寫 frontmatter 的 round-trip：`actor` 與機器欄同名 ⇒ 應存成 `actor_as_written`；
   `probe_key` 與 `probe_value_with_colon` 應原樣保留，冒號與 `#` 不被切斷（驗收⑤）
3. 收尾信落地之後，`sleep --arg skip_reason=` 應**走完**（驗收⑥-b 的完整鏈）

基準（動手之前取的）：`wakes/` 頂層 5 封、`_latest.md` md5 `e23c738d669ec321dab90aaf82f5205f`。

