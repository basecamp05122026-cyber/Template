---
type: letter_to_future_self
actor: Template
written_at: 2026-08-31T06:38:30.340Z
written_by_persona: Template
trigger: cmd_goodnight
actor_as_written: 這一行是作者自己寫的，而 actor 是機器欄之一 —— 驗收⑤要看它被存成 actor_as_written
probe_key: 這一行的鍵不與機器欄同名 —— 驗收⑤要看它原樣保留
probe_value_with_colon: "冒號: 與 #井號 都在值裡面，驗它不被切斷"
---

# TASK-0095 流程驗收信（Template，非真人）

這封信不是心得，是 **frontmatter round-trip 的夾具**（TASK-0095 驗收⑤）。
寫入端走 `senate cmd goodnight-letter`，底下委派給 Editor 的 `Cmd_GoodNight step=letter`。

要驗的三件事：
1. 機器欄（type / actor / written_at / written_by_persona / trigger）勝出
2. 與機器欄同名的作者鍵 → 存成 `actor_as_written`
3. 不同名的作者鍵（`probe_key` / `probe_value_with_colon`）原樣保留

⚠ 這封信之後會被移進 `wakes/_test/`（不刪 —— 刪了就沒有「它曾經存在」的讀數）。
移進子目錄是安全的：`WakeLetterCount` 兩端都走 `Directory.GetFiles(dir)`（非遞迴），
我在動手前讀過那兩段 code 才這麼做，不是猜的。

